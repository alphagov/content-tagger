<%= content_for :page_title, "#{page_title} - Content Tagger - GOV.UK" %>

<header class="heading-with-actions">
  <ol class="breadcrumb">
    <% breadcrumbs.each do |crumb| %>
      <li><%= auto_link crumb %></li>
    <% end %>

    <li class="active"><%= active_item %></li>
  </ol>

  <h1><%= raw title %></h1>

  <% if block_given? %>
    <nav class="btn-group">
      <%= yield %>
    </nav>
  <% end %>
</header>

<h3>Related content items</h3>

<p class="explain">
  Related items are displayed in the sidebar. Enter the URL or path of a GOV.UK
  page, such as /pay-vat or /benefit-cap-calculator
</p>

<% unless @tagging_update.ordered_related_items.nil? %>
   <% @tagging_update.ordered_related_items.each do |l| %>
    <div class="form-group">
      <input type="text" name="tagging_update_form[ordered_related_items][]" value="<%= l %>" class="form-control" />
    </div>
  <% end %>

  <% 5.times do %>
    <div class="form-group">
      <input type="text" name="tagging_update_form[ordered_related_items][]" value="" class="form-control" />
    </div>
  <% end %>
<% end %>
